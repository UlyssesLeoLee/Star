// =====================================================================
// frontend/src/app/(app)/analytics/page.tsx — U5 placeholder
//
// Will be replaced by U4 (Agents / Analytics / Inbox / Settings) worker.
// Renders a minimal stub so /analytics is reachable as 200 during the
// U5 deliverable window.
// =====================================================================

export default function AnalyticsPage() {
  return (
    <div className="p-6">
      <h1 className="text-title font-semibold text-ink">Analytics</h1>
      <p className="text-body text-ink-dim mt-2">
        U5 placeholder. U4 will replace with: 6 KPI cards, daily cost
        chart, error mix donut, leaderboard table.
      </p>
    </div>
  );
}
