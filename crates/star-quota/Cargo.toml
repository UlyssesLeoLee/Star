[package]
name = "star-quota"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
description = "STAR Tenant Quota (Phase G.5) — compute / storage / LLM token 配额, 13 類 RLS, M 派生 SCD Type 2"

# G.5 (per `docs/briefs/next-session/OPT-NEXT-04-phase-g.md` §3.5):
# - 资源配额: compute / storage / LLM token
# - 多租户隔离: RLS 13 類 (per 守门 #13 d)
# 守门 #13 d: M = 物理删除禁止 + SCD Type 2 + RLS 13 類必携

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }
thiserror = { workspace = true }
tracing = { workspace = true }
async-trait = { workspace = true }

[lints]
workspace = true
