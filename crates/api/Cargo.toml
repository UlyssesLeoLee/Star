[package]
name = "api"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
# 基础依赖(全部 crate 共享)
serde = { workspace = true }
serde_json = { workspace = true }
async-trait = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }
chrono = { workspace = true }
tokio = { workspace = true }

star-context = { path = "../star-context" }
# P0-2: ApiError 映射 (per WBS §14.15, 0.3M, H2 done 后 unblock 启动)
# 6 domain crate From impls (WorkItemError / WorkspaceError / WorktreeError / SearchError / ScmError / ValidationError)
domain-work-item = { path = "../domain-work-item" }
domain-workspace = { path = "../domain-workspace" }
domain-worktree = { path = "../domain-worktree" }
domain-search = { path = "../domain-search" }
domain-scm = { path = "../domain-scm" }
domain-validation = { path = "../domain-validation" }
# P0-4: infrastructure adapter 错误映射 (per WBS §14.15 P0-4 收官, 0.6M, 跟 P0-2/3 同模式)
infrastructure = { path = "../infrastructure" }
# ARG.4 (P3-C W4): crates/api 扩展 arg 模块 — 13 REST + 1 WebSocket
# per docs/design/DD-AGENT-RELATIONSHIP-001.md v0.1.1 §4.12
# 守门 #48 (2026-09-08 08:22 JST) axum 0.8 web framework lock
axum = { version = "0.8", features = ["ws", "macros"] }
# tokio-stream for broadcast::Receiver Stream impl (sse_hub)
tokio-stream = "0.1"
star-arg = { path = "../arg" }
# P3-D.6 阶段 1 基础 任务 1.4: crates/api/src/agent + canvas_collab
# per docs/briefs/p3-d6-1-4-api-extend.md §1.1 + §2.12
# 守门 #11 缺标比错标: 0 重复加 axum/tokio/serde/uuid/chrono/async-trait/thiserror/star-context
# 守门 #19 v19 累积规不破坏 V0.1 (arg/ 5 file 1863 lines 100% 保留)
agent-domain = { path = "../agent-domain" }
canvas-collab = { path = "../canvas-collab" }
[lints]
workspace = true
