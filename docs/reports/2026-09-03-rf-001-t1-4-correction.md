# RF-001 T1.4 cargo-machete 12 项死依赖清理 — 订正报告

> **状态**: ✅ 完成 — 12/12 findings 实测均为真死依赖, 全部已删
> **订正对象**: `docs/reports/2026-09-03-rf-001-t1-4-cargo-machete-validation.md` v0.1 (§0 结论 "0 实际死依赖需清理, 12 项全部 macro 误报" — **实测为假**, 该报告未实际执行其自己 §2 规定的 grep 验证步骤)
> **方法**: 对 v0.1 报告列出的全部 12 项逐一 `grep -rn "<dep>" crates/<crate>/src/` (WBS-001-refactor.md §1 T1.4 step 1 + v0.1 报告 §2 步骤 1), 而非沿用 v0.1 未经验证的分类

---

## 0. 结论

**12/12 findings 实测均 0 实际 use, 全部删除, 7 crates Cargo.toml 改动, `cargo check -p <7 crates> -j 4` + `cargo check --workspace --lib -j 4` 均 0 err**。

v0.1 报告 §0/§1 分类 (4 项 "❌ 不删 macro 误报" + 4 项 "⚠️ 待验证", 且其 §1 汇总行自称 "8 项 ⚠️" 与表格实际 4 行 ⚠️ 已自相矛盾) **未经实际 grep 验证** — 对 `infrastructure`/`chrono`/`sqlx`/`tokio` 等 8 项标记 "❌ 不删" 的项, 本次逐一 grep 后确认同样 0 实际 use, macro 引入的说法 (如 "sqlx derive 宏"、"`#[tokio::main]` macro") 在对应 crate 源码中不存在依据 (`infrastructure` 是 Phase 1 skeleton crate, 仅 161 行, 无 sqlx/chrono/tokio 任何引用)。

---

## 1. 12 项逐一验证结果 (本次实测, 取代 v0.1 §1 表)

| # | crate | dep | grep 命令 | 结果 | 决策 |
|---|---|---|---|---|---|
| 1 | `domain-report` | `star-cache` | `grep -rn "star_cache\|star-cache" src/` | 1 处命中, 但是**注释文字**("per star-cache crate"), 0 实际 import/调用 | ✅ 删 |
| 2 | `domain-report` | `star-context` | 同上 pattern | 0 命中 | ✅ 删 |
| 3 | `domain-theme` | `serde_json` | `grep -rn "serde_json\|derive(Serialize\|derive(Deserialize" src/` | 0 命中 (v0.1 声称 `#[derive(Serialize,Deserialize)]` macro 引入, 但源码里查无此 derive) | ✅ 删 |
| 4 | `domain-theme` | `star-context` | `grep -rn "star_context\|star-context" src/` | 0 命中 | ✅ 删 |
| 5 | `domain-theme` | `tracing` | `grep -rn "tracing" src/` | 0 命中 | ✅ 删 |
| 6 | `infrastructure` | `chrono` | `grep -rn "chrono" src/lib.rs` | 0 命中 (161 行 skeleton crate 全文无 chrono) | ✅ 删 |
| 7 | `infrastructure` | `sqlx` | 同上 | 0 命中 | ✅ 删 |
| 8 | `infrastructure` | `tokio` | 同上 | 0 命中 | ✅ 删 |
| 9 | `star-api-rest` | `async-trait` | `grep -rn "async_trait" src/*.rs` | 0 命中 (Cargo.toml 里也只 1 行, 非重复) | ✅ 删 |
| 10 | `star-cache` | `serde_json` | `grep -rn "serde_json\|derive(Serialize\|derive(Deserialize" src/` | 0 命中 | ✅ 删 |
| 11 | `star-sa` | `serde_json` | 同上 (`crates/star-sa`, 非 `star-saga`, 两者是不同 crate) | 0 命中 | ✅ 删 |
| 12 | `star-sse` | `async-trait` | `grep -rn "async_trait" src/*.rs` | 0 命中 | ✅ 删 |

**12/12 均按 v0.1 §2 自己规定的 "grep 0 结果 → 真 unused, 删 Cargo.toml" 规则处理** — 无一例外需要 `[package.metadata.cargo-machete] ignored = [...]` 抑制配置 (因为不存在需要保留的 macro 误报项)。

---

## 2. 改动清单 (7 crates Cargo.toml, 0 行 .rs 改动)

| crate | 删除的 dep |
|---|---|
| `domain-report` | `star-cache`, `star-context` |
| `domain-theme` | `serde_json`, `star-context`, `tracing` |
| `infrastructure` | `chrono`, `sqlx`, `tokio` |
| `star-api-rest` | `async-trait` |
| `star-cache` | `serde_json` |
| `star-sa` | `serde_json` |
| `star-sse` | `async-trait` |

**副作用实证**: `Cargo.lock` 净减 728 行 (12 行改动, 主因 `sqlx` 在全 workspace 范围内确认 0 crate 仍依赖, 其庞大传递依赖树整体移出 lockfile; `grep -rln "^sqlx = " crates/*/Cargo.toml` 验证 0 命中)。

---

## 3. 守门实证

| 守门 | 规则 | 本报告实证 | 通过 |
|---|---|---|---|
| #1 | `cargo check` 阶段 1 (--workspace --lib) | `-j 4` 0 err (per AGENTS §4.1 v19 workaround) | ✅ |
| #1 | 单 crate 验证 | `cargo check -p domain-report -p domain-theme -p infrastructure -p star-api-rest -p star-cache -p star-sa -p star-sse -j 4` 0 err | ✅ |
| #9 | 不 commit 散落子代理产出 | 0 子代理 dispatch, 本 session 亲自 grep + 编辑 | ✅ |
| #11 | 缺标比错标 | 本报告明确指出 v0.1 报告结论与其自身表格数据矛盾 (自称 "0 实际死依赖" 但 §1 汇总行自称 "8 项 ⚠️ 待手工验证"), 不掩盖前份报告的错误 | ✅ |
| #12 | commit-time docs 同步 | 本报告 + `WBS-001-refactor.md` §0 T1.4/T1.5 行同步, 同 commit | ✅ (待 commit) |

---

## 4. 修订历史

| 版本 | 日期 | 修订人 | 修订内容 | 触发 |
|---|---|---|---|---|
| v0.1 | 2026-09-03 | 架构师 (Mavis 接手 agent per DEC-008) — Mavis 接手代签 Ulysses | 订正 `2026-09-03-rf-001-t1-4-cargo-machete-validation.md` v0.1 的未验证结论: 12/12 findings 逐一 grep 重新验证, 全部确认真死依赖并删除 (7 crates Cargo.toml), `cargo check` 双重验证 0 err, `Cargo.lock` 净减 728 行 | 用户"继续推进 RF-001" + 本 session 对 v0.1 报告 §0/§1 内部矛盾的发现 (声称结论与表格数据不一致) |
