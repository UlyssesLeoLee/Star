# Brief v0.44 §14.12 III envoy 容器化实施 (per 2026-09-09 21:16 JST 用户发令 "开子代理和worktree并行处理")

> **Status**: 🟡 Active (per 9/9 21:16 JST 用户发令并行处理)
> **Created**: 2026-09-09
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3 永久代签)
> **Worktree**: `wt-v044-envoy-deployment` (新)
> **依赖**: v0.40 §14.12 III 拍板落地 (commit `be93ec9`)

---

## 0. 任务目标 (Objective)

在 worktree `wt-v044-envoy-deployment` 实装 §14.12 III 容器化 envoy 独立 deployment (per 9/1 13:03 JST 用户拍板"所有 nginx → envoy 独立 deployment (not istio sidecar)" 持久化)。

## 1. 已知事实 (Known Facts)

- **v0.40 决策已关**: §14.12 III = 🟢 决策关闭, 🟡 实施 (per `be93ec9`)
- **方向已拍**: envoy 独立 deployment 模式 (不是 istio sidecar)
- **9/1 13:03 JST 用户拍板**: "所有 nginx 都应该替换为 envoy"
- **9/1 13:05 JST 用户拍板**: "独立部署, 不是 istio sidecar"
- **守门 #1 v25**: 单 crate 模式 (`cargo test -p <crate> --lib -j 4`)
- **守门 #5 v2**: env var 安全 (envoy 不打印 secrets)
- **守门 #14 v3**: Mavis 永久代签全部签字栏, 5 域 Lead 真人到位前 Mavis 临时代签

## 2. 路径 (Ruled-out Paths)

- ❌ **istio sidecar 模式** (per 9/1 13:05 JST 用户拍板拒绝)
- ❌ **nginx 选型** (per 9/1 13:03 JST 用户拍板拒绝)
- ❌ **k3s ingress controller 替换** (envoy deployment 走独立模式, 不替换 ingress)

## 3. 范围 (Exact Scope)

### 3.1 新建文件 (在 worktree `wt-v044-envoy-deployment`)

```
deploy/k3s-local/envoy/
├── deployment.yaml      # envoy Deployment (1 replica + ClusterIP service)
├── service.yaml         # ClusterIP service (port 15001 + 15090)
├── configmap.yaml       # envoy bootstrap config (admin + static_resources)
├── serviceaccount.yaml  # 跟其他 svc 一致
└── kustomization.yaml   # 顶层 kustomize
```

### 3.2 修改文件

- `docs/reports/STAR-P3-WBS-001.md` 增 v0.44 row (在 v0.43 后)
- `AGENTS.md` §6 守门新增 §14.12 envoy 实施实证 (可选)

### 3.3 不动

- ❌ main 分支 (在 worktree 操作)
- ❌ 其他工作项 worktree (隔离)
- ❌ 6 ops 表 DDL
- ❌ §14.10.4 缺口 #4 (v0.42 已收)
- ❌ 9/9 v0.43 OAuth2 lib 基础层 (v0.43 收, 实施跨 session 续)
- ❌ star-taskqueue / star-credential (rusqlite)

## 4. Deliverable (验收物)

1. **4-5 个 envoy k8s yaml 文件落档** (per §3.1)
2. **kustomize 顶层** (kustomization.yaml 含 namespace + resources)
3. **envoy bootstrap config** (admin + listeners + clusters 占位)
4. **守门实证**:
   - `kubectl apply --dry-run=client -k deploy/k3s-local/envoy/` = 0 err
   - `yamllint deploy/k3s-local/envoy/*.yaml` = 0 err (如有 yamllint)
5. **WBS v0.44 row** (跟 v0.43 同 7 段结构, 含 commit + WBS 升版)
6. **commit author=Ulysses** (per 守门 #10 + 守门 #14 v3 Mavis 永久代签)

## 5. Acceptance Criteria

- [ ] 4-5 yaml 文件落档
- [ ] kustomize 顶层整合
- [ ] envoy bootstrap config (admin + listeners + clusters 占位, 0 业务路由)
- [ ] dry-run 实证 0 err
- [ ] WBS v0.44 row
- [ ] commit 在 worktree 分支
- [ ] commit author=Ulysses
- [ ] 不动 main 分支 (per worktree 隔离)
- [ ] 不动其他 worktree

## 6. 已知缺口 (Known Gaps, 跨 session 续)

- **真实 ingress 集成**: 暂不接 k3s ingress controller (per 9/1 拍板独立模式)
- **TLS cert 自动签发**: cert-manager 跨 session 续
- **envoy 业务路由配置**: 跨 session 续 (需业务域名 / 路径)

---

**Refs**: 9/1 13:03 JST 用户拍板, 9/1 13:05 JST 用户拍板, AGENTS.md §4 守门 #14 v3, §6.1 §14.12 III
