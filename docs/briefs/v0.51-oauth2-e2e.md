# Brief v0.51 §14.12 IV OAuth2 5 endpoints 端到端闭环 (per 2026-09-10 06:56 JST 用户发令"完成剩余 8 项")

> **Status**: 🟡 Active (per 9/10 06:56 JST 用户发令)
> **Created**: 2026-09-10
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3 永久代签)
> **Worktree**: `wt-v051-oauth2-e2e` (新)
> **依赖**: v0.49 OAuth2 5 路由 wire + 5 域 RBAC (`08ce92c`) + v0.47 handlers.rs (`913325c`)

---

## 0. 任务目标

§14.12 IV OAuth2 5 endpoints 端到端闭环: 5 endpoints 各 ≥ 1 集成测试 (mock OAuth2State + PgPool), PKCE S256 + JWKS + revoke + introspect 实证, 闭环 §14.12 IV phase 2 收尾。

## 1. 已知事实

- v0.47 handlers.rs 19.7KB 已落 (commit `913325c`)
- v0.49 5 路由 wire + rbac.rs 355 行 (commit `08ce92c`)
- v0.43 4 oauth_* Repository + 4 表 DDL (commits `8cf1191` + `6da4404`)
- pkce.rs 5.6KB RFC 7636 S256 + 7 测试 (含官方测试向量)
- keypair.rs 10.9KB + 6 测试 (RSA 2048 占位实现)
- middleware.rs 4.7KB + 5 测试 (BearerError + require_scope/require_role)
- 5 域 RBAC: rbac.rs 355 行 13 test (player/economy/match/social/admin)
- 守门 #5 v2: token 不入 log
- 守门 #6 v2: JwtError::ExpiredToken retriable
- 守门 #25 v25: 默认 no_network_mode=true (CI 不调外部)

## 2. 范围

```
crates/star-api-rest/tests/oauth_integration.rs (新) # 5 endpoints 集成测试
crates/star-api-rest/tests/common/                 # mock OAuth2State + PgPool helpers
crates/star-api-rest/src/auth/oauth/handlers.rs   # 补 integration test 需要的 helper 导出
crates/star-api-rest/src/auth/oauth/keypair.rs    # 真实 RSA keygen 实证 (替换 v0.43 placeholder)
crates/star-api-rest/Cargo.toml [dev-dependencies]  # + sqlx 0.8 test pool
docs/reports/STAR-P3-WBS-001.md                    # 增 v0.51 row
```

## 3. Deliverable

1. **5 endpoints 集成测试** ≥ 1 each:
   - `GET /oauth/authorize?response_type=code&client_id=...&redirect_uri=...&code_challenge=...` → 302 redirect + code
   - `POST /oauth/token` grant_type=authorization_code + code_verifier + client_id → 200 + access_token + refresh_token
   - `GET /.well-known/jwks.json` → 200 + {keys: [{kty, kid, alg, n, e}]}
   - `POST /oauth/introspect` (跨 session 续, 当前 not_implemented 401 OK)
   - `POST /oauth/revoke` → 200
2. **真实 RSA keygen 实证**: keypair.rs 替换 v0.43 placeholder (走 ring/aws-lc-rs 0.17 已知 API)
3. **mock OAuth2State**: test 不用真实 PG, 走 mock repo (per DDD pattern)
4. **WBS v0.51 row** (跟 v0.50 同 7 段结构)
5. **commit author=Ulysses**

## 4. Acceptance Criteria

- 5 endpoints 各 ≥ 1 集成测试 pass
- cargo test -p star-api-rest --lib -j 4 = 51 baseline + ≥ 5 integration = ≥ 56 pass 0 fail 0 回归
- cargo test -p star-api-rest --tests -j 4 (含 integration) = ≥ 5 集成测试 pass
- WBS v0.51 row
- commit 在 worktree 分支
- author=Ulysses

## 5. 守门实证

- 守门 #1 v25 单 crate 模式
- 守门 #5 v2: env var 安全 (JWT_PRIVATE_KEY_PEM 走 env, mock test 用固定值)
- 守门 #6 v2: BearerError::ExpiredToken retriable
- 守门 #14 v3: Mavis 永久代签
- 守门 #19 v19: mock helper Python 化 (test fixture 跨测试复用)
- 守门 #25 v25: 集成测试不发真 HTTP (走 axum::Router::oneshot)

## 6. 已知缺口 (跨 session 续)

- 真实 RSA 2048 keygen (本 commit 用占位 + 0.43 placeholder, 走 ring/aws-lc-rs 0.17 实证 跨 session)
- bcrypt/argon2 client_secret (v0.43 placeholder, 跨 session)
- PG 容器化 (per IT-5-GAPS, 跨 session)
- 5 endpoints 跨 session 续 (introspect 当前 not_implemented 401 OK, DB-backed introspect 跨 session)

---

**Refs**: v0.49 `08ce92c`, v0.47 `913325c`, v0.43 `8cf1191`, RFC 6749/6750/7009/7517/7636/7662, 守门 #5 v2 / #6 v2 / #14 v3 / #19 v19 / #25 v25
