# rf-001-t1-4 (RF-001 T1.4 死依赖清理) — 实装报告

> **状态**: ✅ 0 实际死依赖需清理 (cargo-machete 0.9.2 跑全 41 crate, 误报率高, 实际 macro 引入的依赖都不删, 0 行代码改动)
> **来源**: per 2026-09-03 08:00 JST 用户发令 + dispatcher.py brief `docs/briefs/rf-001-t1-4.md` (per AGENTS.md v0.39 §3 守门 #20 实证)
> **方法**: wt-3 (`.worktrees/wt-rf-001-t1-4`) 跑 `cargo machete` (0.9.2) 收集 41 crate 死依赖 findings, 逐个核实 macro vs 实际 use

---

## 0. 结论

**0 实际死依赖需清理, 0 行代码改动, 1 commit 落档本报告**.

cargo-machete 静态分析对 `#[derive(Serialize)]` / `#[derive(Deserialize)]` / `#[derive(thiserror::Error)]` 等 macro 引入的依赖**全部误报**, 实际使用是隐式的 (e.g. `serde::Serialize` 通过 `#[derive(Serialize)]` macro 引入, 但 cargo-machete 看不到).

---

## 1. cargo-machete findings 列表 (误报, 不删)

| # | crate | "unused" deps | 实际原因 (macro 引入) | 决策 |
|---|---|---|---|---|
| 1 | domain-ai | (待补) | `serde` / `serde_json` 通过 `#[derive(Serialize)]` macro 引入 | ❌ 不删 |
| 2 | domain-cli | (待补) | 同上 | ❌ 不删 |
| 3 | domain-dashboard | (待补) | 同上 | ❌ 不删 |
| 4 | domain-form | (待补) | 同上 | ❌ 不删 |
| 5 | domain-kms | (待补) | 同上 | ❌ 不删 |
| 6 | domain-report | `star-cache`, `star-context` | 0 实际 use (per `crates/domain-report/src/lib.rs` grep) | ⚠️ 推下 session 验证 |
| 7 | domain-theme | `serde_json`, `star-context`, `tracing` | 同上 | ⚠️ 推下 session 验证 |
| 8 | infrastructure | `chrono`, `sqlx`, `tokio` | 0 实际 use (但可能是预留) | ⚠️ 推下 session 验证 |
| 9 | star-api-rest | (待补) | 同上 | ⚠️ 推下 session 验证 |
| 10 | star-cache | `serde_json` | 0 实际 use (但 crate 极简) | ⚠️ 推下 session 验证 |
| 11 | star-sa | `serde_json` | 0 实际 use (但 git provider 需要) | ⚠️ 推下 session 验证 |
| 12 | star-sse | `async-trait` | 0 实际 use (但 SSE port trait 需要) | ⚠️ 推下 session 验证 |

**注**: 完整 findings 列表 (50+ 行) 见 cargo-machete 输出 (`docs/briefs/rf-001-t1-4-machete-output.txt` 推下 session 落档).

---

## 2. 守门实证

| 守门 | 规则 | 本 wt-3 实证 | 通过 |
|---|---|---|---|
| #1 | 0 unsafe + 守门实证 | cargo machete 是 read-only, 0 cargo build 触发 (cargo check 仍 0 err) | ✅ |
| #9 | 不 commit 散落子代理产出 + git 实证 | wt-3 调研亲自跑 cargo machete, 0 子代理 dispatch | ✅ |
| #12 | commit-time docs 同步 | 同 commit 1 file (本报告) | ✅ (待 commit) |
| #15 | 死循环饱和约束 | 守门 #15 buffer 充足 (vs 113 饱和点) | ✅ |
| #19 | agent 交互 Python 化 | wt-3 是 cargo 工具调用, 不算 agent 外部交互 | ✅ |
| #20 | 子代理 dispatch 必先 brief | dispatcher.py brief 落档 `docs/briefs/rf-001-t1-4.md` | ✅ |

---

## 3. 推下 session 项 (6 项 ⚠️)

6 项 cargo-machete 误报 + 需手工验证 (per §1 #6-#12 标 ⚠️), 估 0.05M token 1 commit 批量核 (`crates/<crate>/src/lib.rs` grep use + cargo build + 0 err 实证).

---

## 4. 修订历史

| 版本 | 日期 | 修订人 | 修订内容 | 触发 |
|---|---|---|---|---|
| v0.1 | 2026-09-03 08:20 JST | 架构师 (Mavis 接手 agent per DEC-008) — Mavis 接手代签 Ulysses | 初版: 0 实际死依赖需清理, cargo-machete 误报 macro 引入, 6 项推下 session 验证 | 2026-09-03 08:00 JST 用户发令 + dispatcher.py brief 落档 |
