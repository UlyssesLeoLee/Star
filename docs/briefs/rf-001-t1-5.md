# Brief: rf-001-t1-5

**Agent**: worker
**Phase**: RF-001-T1
**Created**: 2026-09-03 07:59:50

---

RF-001 T1.5 lint warn → deny (0.3M token, 估 ~45 分钟): 根 Cargo.toml [workspace.lints.rust] 3 项改 deny (missing_docs/rust_2018_idioms/unreachable_pub), 本地分支改不直接推 main。cargo build --workspace 收集触发点, 逐 crate 修 (主要补 missing_docs, 少量 unreachable_pub 判断是否私有化)。全部修完后 1 commit 切 deny。守门 #1 实证 cargo check --workspace --all-targets 0 err (跟当前 baseline 一致)。
