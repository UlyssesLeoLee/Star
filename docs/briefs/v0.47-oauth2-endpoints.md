# Brief v0.47 §14.12 IV OAuth2 5 endpoints + middleware extractor 收官 (per 2026-09-09 22:18 JST 用户发令"开子代理和 worktree 并行处理完成后续任务")

> **Status**: 🟡 Active (per 9/9 22:18 JST 用户发令)
> **Created**: 2026-09-09
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3 永久代签)
> **Worktree**: `wt-v047-oauth2-endpoints` (新)
> **依赖**: v0.43 OAuth2 server lib 基础层 (`8cf1191` + handlers.rs 19.7KB 已写未 commit)

---

## 0. 任务目标 (Objective)

在 worktree `wt-v047-oauth2-endpoints` 落地 §14.12 IV OAuth2 5 endpoints (authorize + token + jwks + introspect + revoke) + Bearer middleware extractor + 5 域 RBAC scope helper, 闭环 v0.43 lib 基础层 → phase 2 实施。

## 1. 已知事实 (Known Facts)

- **v0.43 lib 基础层已收**: commit `8cf1191` + `6da4404` (WBS v0.43)
- **handlers.rs 19.7KB 已写**: 未 commit, 在 `crates/star-api-rest/src/auth/oauth/handlers.rs` (per 9/9 21:23 JST 实证, struct 字段完整化未完)
- **AuthError 5-variant + JWTError 4-variant 已收**: per v0.41 + v0.43
- **OAuth2 5 endpoints 范围**:
  1. `GET /oauth/authorize` — Authorization Code flow (含 PKCE S256)
  2. `POST /oauth/token` — authorization_code / client_credentials / refresh_token 3 grants
  3. `GET /.well-known/jwks.json` — Public key JWKS (RFC 7517)
  4. `POST /oauth/introspect` — RFC 7662
  5. `POST /oauth/revoke` — RFC 7009
- **axum 0.8 macros 缺**: per 9/9 21:08 JST 实证 (E0433 async_trait in axum), 需 `axum = { version = "0.8", features = ["macros"] }`
- **守门 #1 v25**: 单 crate 模式 (`cargo test -p star-api-rest --lib -j 4`)
- **守门 #5 v2**: env var 安全 (private_key_pem 走 env)
- **守门 #6 v2**: JwtError::ExpiredToken retriable
- **守门 #14 v3**: Mavis 永久代签
- **守门 #19 v19**: Python 化

## 2. 路径 (Ruled-out Paths)

- ❌ **lib 基础层** (v0.43 已收, 不重写)
- ❌ **4 Repository** (v0.43 已收, 不重写)
- ❌ **4 表 DDL** (v0.43 已落档 db/migrations, 不重写)
- ❌ **main 分支** (在 worktree 操作)
- ❌ **v0.43 JWT lib 基础层** (JwtError + Claims + JwtConfig + issue_token + verify_token + AuthUser, 不动)
- ❌ **6 ops 表 DDL** (v0.42 已落档, 不动)
- ❌ **5 ops Repository** (v0.42 已收, 不动)
- ❌ **envoy deployment / ARG docs / 守门 v3x** (v0.44/v0.45/v0.46 已收, 不动)
- ❌ **v0.41 缺漏修** (v0.43 已修, 不动)

## 3. 范围 (Exact Scope)

### 3.1 修改文件 (在 worktree `wt-v047-oauth2-endpoints`)

```
crates/star-api-rest/src/auth/oauth/handlers.rs    # 19.7KB 落档 (5 endpoints + BearerError + 4 grant 处理)
crates/star-api-rest/src/auth/oauth/middleware.rs  # 加 AuthenticatedUser extractor (axum::async_trait macros)
crates/star-api-rest/src/auth/oauth/mod.rs        # 加 pub mod handlers;
crates/star-api-rest/src/auth/oauth/keypair.rs    # 真实 RSA keygen (per openssl / aws-lc-rs 0.43 placeholder 替换)
crates/star-api-rest/Cargo.toml                   # axum = { version = "0.8", features = ["macros"] }
crates/star-api-rest/src/lib.rs                    # 加 OAuth2 5 endpoints 路由 (merge 到 router)
docs/reports/STAR-P3-WBS-001.md                   # 增 v0.47 row
```

### 3.2 新建文件

```
crates/star-api-rest/tests/oauth_integration.rs   # 5 endpoints 集成测试 (mock OAuth2State)
```

### 3.3 不动

- ❌ main 分支
- ❌ 任何 v0.1-v0.46 修订历史 (per 守门 #1 禁回溯叙事)
- ❌ star-taskqueue / star-credential (rusqlite)
- ❌ 其他 worktree

## 4. Deliverable (验收物)

1. **handlers.rs 完整化**: 5 endpoints + 4 grant 处理 (authorization_code / client_credentials / refresh_token) + BearerError 6-variant + IntoResponse impl
2. **middleware.rs 增强**: AuthenticatedUser extractor (axum::async_trait + FromRequestParts) + 5 域 RBAC scope helper
3. **keypair.rs 增强**: 真实 RSA 2048 keygen (替换 0.43 placeholder)
4. **5 endpoints 路由 wire**: 接到 star-api-rest router (跟 health/webhook 路由同 pattern)
5. **集成测试**: `oauth_integration.rs` 5 endpoints 各 ≥ 1 test
6. **WBS v0.47 row**
7. **commit author=Ulysses** (per 守门 #10 + 守门 #14 v3 Mavis 永久代签)

## 5. Acceptance Criteria

- [ ] handlers.rs 5 endpoints 完整化 + struct 字段完整 (per oauth_* Repository 24/25 字段)
- [ ] middleware.rs AuthenticatedUser extractor 落地 (axum::async_trait 派生)
- [ ] keypair.rs 真实 RSA keygen 落地 (replace placeholder)
- [ ] 5 endpoints 路由 wire (跟 health 同 pattern)
- [ ] 集成测试 ≥ 5 case
- [ ] WBS v0.47 row
- [ ] commit 在 worktree 分支
- [ ] author=Ulysses
- [ ] 不动 main 分支

## 6. 守门实证 (per 守门 #1 v25 单 crate 模式)

- `cargo test -p star-api-rest --lib -j 4` = 29 baseline + ≥ 5 OAuth2 endpoints = **≥ 34 pass 0 fail 0 回归**
- `cargo check --workspace --lib -j 4` = **0 err**
- `cargo fmt -p star-api-rest` = **0 err**
- 守门 #5 v2: private_key_pem 走 env var, 错误不含 key 内容
- 守门 #6 v2: BearerError::ExpiredToken retriable
- 守门 #14 v3: Mavis 永久代签

## 7. 已知缺口 (Known Gaps, 跨 session 续)

- **5 域 RBAC 实证**: 跟 claims.roles 整合 (估 1M tokens 跨 session 续)
- **bcrypt/argon2 client_secret 实证**: 0.43 placeholder, 跨 session 续
- **真实 RSA 验签**: aws-lc-rs 或 openssl 实证 (估 0.05M tokens 跨 session 续)
- **PG 容器化**: 5/6 Repository 真实 PG container (per IT-5-GAPS 缺口 #1)

---

**Refs**: 9/9 22:18 JST 用户发令, handlers.rs 19.7KB 9/9 21:23 JST 写未 commit, v0.43 OAuth2 lib 基础层 `8cf1191`, 守门 #1 v25 / #5 v2 / #6 v2 / #14 v3 / #19 v19
