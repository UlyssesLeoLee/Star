# Brief v0.55 §14.17 H2-EXT 5 域类型修复 (per 2026-09-10 08:14 JST 用户发令"继续")

> **Status**: 🟡 Active
> **Created**: 2026-09-10
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3)
> **Worktree**: `wt-v055-h2-ext-final` (新)

---

## 0. 任务目标

§14.17 H2-EXT 5 域 (comment/identity/project/tenant/work-item) 类型修复 + workspace_ids/tenant_policy_id 字段扩展 + DeviceId → Uuid Copy 语义。Root session 兜底(per v0.52 worker 17 分钟未 commit 实证,本批根 session 直接做)。

## 1. 已知事实

- HANDOFF-ST-001 v0.5 §5.1: 5 域 (comment/identity/project/tenant/work-item) 实证
- v0.34 H3 已收 4 macros `as_uuid` Copy + 1 caller deref
- v0.48 H1/H4/H5 已收 1 domain-theme error 一致化 (commit `39771db`)
- v0.55 实证 0 域代码 (H2-EXT 5 域跟 H3 同 pattern 但 5 域)

## 2. 范围

```
crates/domain-comment/src/lib.rs + src/macros.rs      # DeviceId → Uuid
crates/domain-identity/src/lib.rs + src/macros.rs     # tenant_policy_id 加
crates/domain-project/src/lib.rs + src/macros.rs      # workspace_ids 加
crates/domain-tenant/src/lib.rs + src/macros.rs       # tenant_policy_id 加
crates/domain-work-item/src/lib.rs + src/macros.rs    # DeviceId → Uuid
```

## 3. Deliverable

1. **5 domain 类型修复**:
   - domain-comment: DeviceId → Uuid (per H3 pattern)
   - domain-identity: tenant_policy_id 字段
   - domain-project: workspace_ids 字段 (Vec<Uuid>)
   - domain-tenant: tenant_policy_id 字段
   - domain-work-item: DeviceId → Uuid
2. **macros 统一 as_uuid Copy**: 跟 H3 同
3. **跨 domain 调用修 deref**: per H3 §3
4. **WBS v0.55 row**
5. **commit author=Ulysses**

## 4. Acceptance Criteria

- 5 domain 修复完
- cargo test -p <domain> --lib -j 4 = 0 fail 0 回归
- WBS v0.55 row
- commit author=Ulysses

## 5. 已知缺口 (跨 session 续)

- H2 原 3 域 service.rs 改造 (~150+ call sites) 跨 session
- 完整 H2-EXT cross-domain saga 跨 session

---

**Refs**: HANDOFF-ST-001 v0.5 §5.1, v0.34 H3 `e44c934`, v0.48 H1/H4/H5 `39771db`, 守门 #14 v3
