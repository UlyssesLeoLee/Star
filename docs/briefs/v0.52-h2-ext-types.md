# Brief v0.52 §14.17 H2-EXT #4 #5 类型不兼容修复 (per 2026-09-10 07:24 JST 用户发令"继续")

> **Status**: 🟡 Active (per 9/10 07:24 JST 用户发令)
> **Created**: 2026-09-10
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3 永久代签)
> **Worktree**: `wt-v052-h2-ext-types` (新)
> **依赖**: HANDOFF-ST-001 v0.5 §5.1, H3 commit `e44c934` (v0.34)

---

## 0. 任务目标

HANDOFF-ST-001 v0.5 §5.1 实证 5 域 (5 domain: comment/identity/project/tenant/work-item) 类型不兼容 + workspace_ids/tenant_policy_id 字段扩展, 跟 H3 `as_uuid` Copy 语义同模式。修复实证 0 err 0 回归。

## 1. 已知事实

- HANDOFF-ST-001 v0.5 §5.1: 5 域 (comment/identity/project/tenant/work-item) 实际是 8 域 (3 + H2-EXT 5), 估 0.3-0.5M tokens (实证) / 1.1-1.6M 实测
- v0.34 H3 已收 4 macros (`as_uuid` Copy) + 1 caller deref
- v0.48 H1/H4/H5 已收 1 domain-theme error 一致化 (commit `39771db`)
- 守门 #1 v25: 单 crate 模式
- 守门 #14 v3: Mavis 永久代签

## 2. 范围

```
crates/domain-comment/src/                # DeviceId → Uuid + workspace_ids 扩展
crates/domain-identity/src/               # tenant_policy_id 字段
crates/domain-project/src/                # workspace_ids 扩展
crates/domain-tenant/src/                 # tenant_policy_id 字段
crates/domain-work-item/src/              # DeviceId → Uuid
```

## 3. Deliverable

1. **5 domain DeviceId/字段扩展**: H2-EXT 5 域修复
2. **H3 同模式 as_uuid Copy**: 5 域 macros 统一
3. **跨 domain 调用修 deref**: per H3 §3 caller deref 模式
4. **cargo test -p <domain> --lib -j 4 = 0 fail 0 回归** (每个改的 domain)
5. **WBS v0.52 row**
6. **commit author=Ulysses**

## 4. Acceptance Criteria

- 5 domain 修复完
- cargo test 全 0 fail 0 回归
- WBS v0.52 row
- commit 在 worktree 分支
- author=Ulysses

## 5. 已知缺口 (跨 session 续)

- 完整 H2-EXT cross-domain saga (估 1-2M tokens)
- H2 原 3 域 service.rs 改造 (~150+ call sites) 跨 session

---

**Refs**: HANDOFF-ST-001 v0.5 §5.1, v0.34 H3 `e44c934`, v0.48 H1/H4/H5 `39771db`, 守门 #14 v3
