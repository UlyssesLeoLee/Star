# Brief v0.49 §14.12 IV OAuth2 5 endpoints 路由 wire + 5 域 RBAC 实证 (per 2026-09-10 01:36 JST 用户发令"继续")

> **Status**: 🟡 Active (per 9/10 01:36 JST 用户发令)
> **Created**: 2026-09-10
> **Authority**: Ulysses（一人公司 12 角色 per DEC-008）— Mavis 接手 (per 守门 #14 v3 永久代签)
> **Worktree**: `wt-v049-oauth2-router` (新)
> **依赖**: v0.47 OAuth2 5 endpoints + middleware (`913325c`)

---

## 0. 任务目标

把 v0.43 + v0.47 已写的 5 endpoints (authorize + token + jwks + introspect + revoke) + Bearer middleware 接到 star-api-rest router, 加 5 域 RBAC 实证 (per 守门 #14 v2 5 域 Lead 拍板 跨 session 续)。

## 1. 已知事实

- v0.47 commit `913325c` 落 handlers.rs 19.7KB + middleware/keypair 增强
- v0.43 commit `8cf1191` 落 lib 基础层 + 4 oauth Repository
- handlers.rs 5 endpoints 已在 `crates/star-api-rest/src/auth/oauth/handlers.rs`
- 5 域 Lead (player / economy / match / social / admin) per 守门 #3 v2 + 9/3 11:35 JST Mavis 临时代签
- 守门 #14 v3: Mavis 永久代签全部签字栏

## 2. 范围

```
crates/star-api-rest/src/lib.rs                  # 加 5 路由 wire (per /oauth/authorize + /oauth/token + /.well-known/jwks.json + /oauth/introspect + /oauth/revoke)
crates/star-api-rest/src/auth/oauth/middleware.rs # axum::async_trait macros feature + AuthenticatedUser extractor
crates/star-api-rest/Cargo.toml                  # axum = { version = "0.8", features = ["macros"] }
crates/star-api-rest/src/rbac.rs (新)            # 5 域 RBAC helper (per 守门 #14 v2 5 域 Lead 拍板, Mavis 临时代签)
```

## 3. Deliverable

1. **5 路由 wire**: `lib.rs` 加 `.route("/oauth/authorize", ...) + /oauth/token + /.well-known/jwks.json + /oauth/introspect + /oauth/revoke`
2. **AuthenticatedUser extractor**: middleware.rs 加 `FromRequestParts` 实现
3. **5 域 RBAC helper**: `rbac.rs` 新建, 5 域 × 9 SA × claims.roles 检查
4. **集成测试**: 5 路由 ≥ 5 case
5. **WBS v0.49 row**
6. **commit author=Ulysses**

## 4. Acceptance Criteria

- 5 路由 wire (router_contains_expected_paths 含 5 oauth 路由)
- cargo test -p star-api-rest --lib -j 4 = 33 baseline + 5 路由 = ≥ 38 pass 0 fail 0 回归
- WBS v0.49 row
- commit 在 worktree 分支
- author=Ulysses

## 5. 已知缺口

- 真实 RSA 验签 跨 session 续 (per 守门 #25 v25 owner 拍板)
- bcrypt client_secret 跨 session 续
- PG 容器化 跨 session 续

---

**Refs**: v0.47 `913325c`, v0.43 `8cf1191`, 守门 #14 v2/v3
