docs(requirements): 补登记 3 项 traceability + 新增 2 项真实缺口 REQ

第一类（后端已实现，requirements.md 漏登记）：

- REQ-WF-003（§8.2）：WorkItem 状态转换可配置 Guard（角色 / 人工批准 / Validation 通过），
  由 Application/Authorization 层强制执行；与 REQ-PERM-002 一致。已实现于
  crates/domain-workflow/src/lib.rs:134-148（enum Guard + 3 变体）、618/626/634
  （执行时校验）、1384（使用示例）。P0 升级，因直接关联 ARCH-OBL-DEV-001/002
  （Agent 授权边界）。

- REQ-PLAN-007（§9）：Milestone / Roadmap。Milestone 用于对一组 WorkItem 打分组
  标签并设定共同 due_date，Roadmap 是只读 Projection。明确不含发布语义（无
  release_date / released 标记），与 Jira Fix Version 差异点；后续如需"PR 属于
  哪次发布"追溯需另开 REQ-PLAN-008。已实现于 crates/domain-planning/src/lib.rs:335-346
  （struct Milestone）与 docs/api-design.md:429（GET /v1/projects/{id}/roadmap）。

- REQ-WI-001（§8.1）：WorkItem 支持自由文本 Labels 与 Components 分类，Components
  可作为 AI Task Repository Scope 粗粒度前置（实际生效仍以 AgentPolicy / Worktree
  授权边界为准）。已实现于 crates/domain-work-item/src/entity.rs:100,103、
  lib.rs:116-117,329、service.rs:146-147。

§41.1 ID 前缀表与 §41 末尾前缀清单同步新增 `WI-xxx`。
§41.2 P0 Requirement 登记表新增 WF-003。

第二类（代码与需求文档均无，真实功能缺口）：

- REQ-NOTIF-003（§12）：WorkItem 支持 Watcher 列表，用户自行加入/退出，Watcher
  收到的通知不受 REQ-NOTIF-002 全局降噪策略限制。代码全 crates/ 无 watcher 命中。

- REQ-AUTO-003（§11，V1 候选）：批量操作（Bulk Transition / Assign / Cancel），
  每条独立经过 REQ-WF-003 Guard 校验，不得绕过单条授权。代码全 crates/ 无
  bulk/batch 命中。

§30 冲突检查：5 条新 REQ 均不与 MVP Must Have 冲突；REQ-AUTO-003 / REQ-NOTIF-003
已标 V1 候选；REQ-WF-003 / REQ-PLAN-007 / REQ-WI-001 为已实现补登记。
